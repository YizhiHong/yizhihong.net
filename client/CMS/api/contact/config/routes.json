{
  "routes": [
    {
      "method": "GET",
      "path": "/contacts",
      "handler": "Contact.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contacts/count",
      "handler": "Contact.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/contacts/:_id",
      "handler": "Contact.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/contacts",
      "handler": "Contact.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contacts/:_id",
      "handler": "Contact.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contacts/:_id",
      "handler": "Contact.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
